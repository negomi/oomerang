<!DOCTYPE html>
<html>
<head>
  <title>oomerang | a new kind of lost &amp; found</title>
  <script type="text/javascript" src="https://maps.googleapis.com/maps/api/js?key=AIzaSyBEi9htZN3uanHiVC90BoxZzy_VeF5In5k&sensor=true&libraries=places">
  </script>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
  <meta content="initial-scale=1.0, maximum-scale=1.0, user-scalable=0" name="viewport">
  <meta content="yes" name="apple-mobile-web-app-capable">
</head>
<body>

<!-- Modals -->
<%= render "sign_in_modal" %>
<%= render "sign_up_modal" %>
<%= render "account_modal" %>

    <nav class="navbar navbar-inverse navbar-fixed-top" role="navigation">
        <a href="/"><span class="navbar-brand">oomerang</span></a>
        <ul class="nav navbar-nav navbar-right">
            <li class="dropdown">
                <a href="#" class="dropdown-toggle" data-toggle="dropdown"><span class="glyphicon glyphicon-user"></span></a>
                <ul class="dropdown-menu">
                    <% if user_signed_in? %>
                        <li><%= link_to "Sign out", destroy_user_session_path, :method => :delete %></li>
                        <li class="divider"></li>
                        <li><%= link_to "Account (non modal)", edit_user_registration_path %></li>
                        <li><a href="#" data-toggle="modal" data-target="#accountModal">Account</a></li>
                    <% else %>
                        <li><a href="#" data-toggle="modal" data-target="#signUpModal">Sign up</a></li>
                        <li><a href="#" data-toggle="modal" data-target="#signInModal">Sign in</a></li>
                    <% end %>
                </ul>
            </li>
        </ul>
    </nav><!-- End nav -->

    <%= yield %>

    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>

</body>
</html>
